#!/bin/bash

declare options=("suspend
shutdown
reboot
escswap
red+
red0")

choice=$(echo -e "${options[@]}" | dmenu -i -p 'run')

case "$choice" in
	suspend)
		slock & systemctl suspend
	;;
	shutdown)
		shutdown now
	;;
	reboot)
		reboot
	;;
	escswap)
		~/.scripts/escswap
	;;
	red+)
		redshift -O 3600
	;;
	red0)
		redshift -x
	;;
	*)
		exit 1
	;;
esac
